---
name: skill-01g
title: 标注符号集合原则
description: 标点符号、标注符号、Markdown符号三大符号集合的完全分离原则。定义符号使用规范，防止符号冲突，确保文本解析的确定性。
version: 1.2
last_updated: 2026-04-05
---

# SKILL 01g: 标注符号集合原则

> **核心原则**：标点符号、标注符号、Markdown符号三大集合完全分离（disjoint），互不重叠，确保文本解析的确定性和可维护性。

---

## 一、问题诊断

### 1.1 为什么需要Disjoint原则

**符号冲突的危害**：
1. **解析歧义**：同一符号在不同上下文有不同含义，导致解析器混淆
2. **渲染错误**：标注符号被误认为Markdown语法，导致显示异常
3. **维护困难**：人工阅读时难以区分符号的作用域
4. **脚本脆弱**：正则表达式需要大量转义和特殊处理

**示例（符号冲突）**：
```markdown
❌ 问题1：标点与Markdown冲突
《史记》曰："天下大势，分久必合。"
（引号" "如果用ASCII双引号，会与Markdown代码块冲突）

❌ 问题2：标注与标点冲突
〖@项羽〗曰："吾闻〖@汉王〗。"
（引号与标注符号混用，难以区分层次）

❌ 问题3：标注与Markdown冲突
〖$三万人〗出*兵*
（$与Markdown数学公式冲突，*与强调符号冲突）
```

### 1.2 Disjoint原则的优势

✅ **解析确定性**：每个符号有唯一的语义，解析器行为可预测
✅ **渲染稳定性**：不会误触Markdown/HTML渲染规则
✅ **维护清晰性**：人类和机器都能准确识别符号作用
✅ **脚本简洁性**：正则表达式简单，无需复杂转义

---

## 二、三大符号集合定义

### 2.1 标点符号集合（Punctuation Set）

**定义**：用于断句、语气、引用的符号，属于原文的一部分。

**完整列表**（13类）：

| 符号 | Unicode | 名称 | 用途 |
|------|---------|------|------|
| 。 | U+3002 | 全角句号 | 句子结束 |
| ，| U+FF0C | 全角逗号 | 句内停顿 |
| 、 | U+3001 | 顿号 | 并列词语 |
| ；| U+FF1B | 全角分号 | 复句分隔 |
| ：| U+FF1A | 全角冒号 | 提示总结 |
| ？| U+FF1F | 全角问号 | 疑问句 |
| ！| U+FF01 | 全角叹号 | 感叹句 |
| " | U+201C | 左双引号 | 第一层引用开始 |
| " | U+201D | 右双引号 | 第一层引用结束 |
| ' | U+2018 | 左单引号 | 第二层引用开始 |
| ' | U+2019 | 右单引号 | 第二层引用结束 |
| 《 | U+300A | 左书名号 | 书名开始 |
| 》 | U+300B | 右书名号 | 书名结束 |
| —— | U+2014×2 | 破折号 | 注释转折 |
| …… | U+2026×2 | 省略号 | 文字省略 |

**规范**：
- ✅ 只使用全角标点（CJK Punctuation范围）
- ❌ 禁止半角标点：`,` `.` `;` `:` `?` `!` `"` `'`
- ❌ 禁止ASCII引号：`"` `'` `<` `>`
- ❌ 禁止直角引号：「」『』（日文用法）

### 2.2 标注符号集合（Annotation Set）

**定义**：用于语义标记的符号，不属于原文，用于知识抽取。

#### 2.2.1 实体标注符号（〖TYPE〗格式）

**外层括号**：
- 左括号：`〖`（U+3016，白色六角括号左）
- 右括号：`〗`（U+3017，白色六角括号右）

**类型标记符号**（18种）：

> **📌 邦国符号变更历史**：
> - **v1.0-v2.x** (2026-03-31前)：使用 `'` (U+0027, 单引号) 标注邦国
> - **v3.0** (2026-04-01起)：迁移至 `◆` (U+25C6, 黑菱形)
> - **迁移原因**：单引号与标点符号冲突，且不符合Disjoint原则
> - **⚠️ 已知问题**：`◆` 与动词标注 `◈` 视觉相似，建议未来进一步优化
> - **详细记录**：见 [SKILL_03a1_标注符号迁移.md](references/SKILL_03a1_标注符号迁移.md)

| 符号 | 类型 | 说明 | Markdown冲突 |
|------|------|------|-------------|
| `@` | 人名 | 历史人物 | - |
| `=` | 地名 | 城邑山川 | - |
| `;` | 官职 | 正式职衔 | - |
| `#` | 身份 | 社会角色 | ⚠️ 标题符号 |
| `%` | 时间 | 年号纪年 | - |
| `&` | 氏族 | 宗族家族 | - |
| `◆` | 邦国 | 政治实体 | ⚠️ 与动词`◈`视觉相似 |
| `^` | 名物 | 典章名物（含礼制/律历/医学等专门术语） | - |
| `~` | 族群 | 民族部落 | ⚠️ 删除线（某些扩展） |
| `•` | 器物 | 礼器兵器 | - |
| `!` | 天文 | 星象历法 | - |
| `?` | 神话 | 传说神话 | - |
| `+` | 生物 | 动植物 | - |
| `{` | 典籍 | 古籍书名 | ⚠️ 属性语法（某些扩展） |
| `:` | 礼仪 | 礼仪祭祀 | ⚠️ 自定义块 `:::` |
| `[` | 刑法 | 刑罚法律 | ⚠️ 链接/段落编号 |
| `_` | 思想 | 哲学概念 | ⚠️ 斜体 |
| `$` | 数量 | 计量数值 | ⚠️ 数学公式（某些扩展） |

**⚠️ 与Markdown有交集的符号（需要未来修正）**：
- `#`：Markdown标题符号（`# 标题`），但在 `〖#身份〗` 中被括号包裹，实际无冲突
- `_`：Markdown斜体符号（`_text_`），在 `〖_思想〗` 中无冲突，但不直观
- `[`：Markdown链接和段落编号（`[1]`），在 `〖[刑法〗` 中可能引起混淆
- `$`：Markdown数学公式（`$E=mc^2$`，某些渲染器），在 `〖$数量〗` 中无冲突
- `:`：Markdown自定义块（`:::`），在 `〖:礼仪〗` 中无冲突
- `{`：Markdown属性语法（`{.class}`，某些扩展），在 `〖{典籍〗` 中无冲突
- `~`：Markdown删除线（`~~text~~`，某些扩展），在 `〖~族群〗` 中无冲突

**当前安全性评估**：
- ✅ **短期可用**：因为类型标记在 `〖〗` 内部，Markdown解析器不会识别
- ⚠️ **长期建议**：未来版本应替换有冲突的符号为更独特的Unicode字符
- 🔄 **优先级**：低（当前设计可用，但影响可读性和直观性）

**消歧分隔符**：
- `|`（U+007C，竖线）：用于`〖TYPE 显示名|规范名〗`
- ⚠️ 与Markdown表格符号冲突，但在 `〖〗` 内部无实际问题

#### 2.2.2 修辞标注符号（〘TYPE〙格式）

**外层括号**：
- 左括号：`〘`（U+3018，白色龟甲括号左）
- 右括号：`〙`（U+3019，白色龟甲括号右）

**设计理由**：修辞标注属于**语言层**（linguistic layer），与实体层（世界中的对象）、事件层（世界中的行为）平级但性质不同。成语、典故等固化语言单元不指称具体实体，而是文本作为语言本身的特征。

**类型标记符号**（1种，暂定）：

| 符号 | 类型 | 说明 | Unicode | 古籍出现 |
|------|------|------|---------|---------|
| `※` | 成语 | 固化四字熟语、固定短语 | U+203B | ❌ 绝不出现 |

**示例**：
```
〘※移风易俗〙   〘※名山大川〙
```

**未来扩展预留**（暂不启用）：
- `✦`（U+2726）典故
- `✧`（U+2727）谚语
- `⁂`（U+2042）其他修辞

#### 2.2.3 动词标注符号（⟦TYPE⟧格式）

**外层括号**：
- 左括号：`⟦`（U+27E6，数学左白方括号）
- 右括号：`⟧`（U+27E7，数学右白方括号）

**类型标记符号**（4种）：

| 符号 | 类型 | 说明 | Unicode | 视觉特征 |
|------|------|------|---------|---------|
| `◈` | 军事 | 攻伐征战 | U+25C8 | 白色菱形含黑点 |
| `◉` | 政治 | 立废诛封 | U+25C9 | 鱼眼圆形 |
| `○` | 外交 | 使朝聘盟 | U+25CB | 白圆 |
| `◇` | 经济 | 赐赋税贸 | U+25C7 | 白菱形 |

**⚠️ 视觉混淆警告**：
- **问题符号对**：`〖◆周〗`（实体-邦国）vs `⟦◈攻〗`（动词-军事）
- **混淆原因**：两者都是菱形系列，`◆`（黑菱形）和`◈`（白菱形含点）在快速阅读时难以区分
- **影响范围**：邦国实体（周、齐、秦等）经常与军事动词（攻、伐等）共现，如"〖◆秦〗⟦◈攻〗〖◆赵〗"
- **改进建议**：见 §2.2.3 实体-动词符号协调设计

#### 2.2.3 实体-动词符号协调设计原则 ⚠️

**核心问题**：实体标注和动词标注的TYPE符号视觉相似，导致快速阅读时混淆。

**设计原则**（未来符号选择时必须遵守）：

1. **形状系列分离原则**
   - ✅ 实体符号使用一个形状系列（如：圆形系）
   - ✅ 动词符号使用另一个形状系列（如：方形系）
   - ❌ 禁止两类符号使用相同形状系列（如都用菱形）

2. **视觉区分度测试**
   - 在实际文本中并列出现时，能在0.5秒内区分
   - 测试案例：`〖TYPE1实体〗⟦TYPE2动词〗〖TYPE3实体〗`
   - 通过标准：10人测试，错误率<10%

3. **语义关联优先级**
   - P1：视觉区分度（必须）
   - P2：符号语义关联（建议）
   - P3：输入便捷性（考虑）

**当前问题分析**：

| 维度 | 实体标注 | 动词标注 | 冲突分析 |
|-----|---------|---------|---------|
| **外层括号** | `〖〗` | `⟦⟧` | ✅ 明显不同 |
| **符号系列** | 混合（ASCII+菱形） | 菱形+圆形 | ⚠️ 菱形系冲突 |
| **问题符号** | `◆` (邦国) | `◈` (军事) | ❌ 都是菱形 |
| **共现频率** | 高 | 高 | ⚠️ 经常并列 |

**改进方案（建议）**：

**方案A：替换邦国符号**（推荐，影响范围小）
- 将 `◆` 替换为非菱形系符号
- 候选符号：`⊚` (U+229A, 环形点) 或 `◎` (U+25CE, 靶心圆)
- 优势：只需迁移邦国标注（1种实体类型）
- 劣势：需要再次执行符号迁移

**方案B：重新设计动词符号**（影响大）
- 将菱形系动词符号（`◈◇`）替换为方形系
- 优势：动词符号更统一（全用方形）
- 劣势：需迁移4种动词标注，影响范围大

**方案C：保持现状，加强视觉提示**（临时）
- 在HTML渲染时，实体和动词使用不同底色
- 实体：浅蓝底色，动词：浅黄底色
- 优势：无需迁移，立即生效
- 劣势：纯文本环境下仍然混淆

**推荐实施路径**：
1. **短期**：方案C（HTML渲染优化）
2. **中期**：方案A（邦国符号迁移至 `⊚` 或 `◎`）
3. **长期**：全面审查所有18种实体+4种动词符号，建立完整的视觉区分矩阵

### 2.3 Markdown符号集合（Markdown Set）

**定义**：用于文档结构和格式的符号，属于标记语言语法。

**完整列表**：

| 符号 | 用途 | 示例 |
|------|------|------|
| `#` | 标题 | `# 一级标题` |
| `##` | 二级标题 | `## 二级标题` |
| `**` | 粗体 | `**文字**` |
| `*` | 斜体/列表 | `*斜体*` 或 `* 列表项` |
| `-` | 列表 | `- 列表项` |
| `>` | 引用 | `> 引用内容` |
| `[` `]` | 链接/段落编号 | `[1]` `[链接](url)` |
| `` ` `` | 行内代码 | `` `代码` `` |
| ```` ``` ```` | 代码块 | ` ```代码块``` ` |
| `:::` | 自定义块 | `::: 太史公曰` |
| `---` | 分隔线 | `---` |
| `|` | 表格 | `| 列1 | 列2 |` |

---

## 三、Disjoint原则规范

### 3.1 核心原则

**完全分离原则**：
```
P ∩ A ∩ M = ∅
```
其中：
- P = 标点符号集合（Punctuation）
- A = 标注符号集合（Annotation）
- M = Markdown符号集合（Markdown）

**任意两两不相交**：
```
P ∩ A = ∅  （标点与标注不重叠）
P ∩ M = ∅  （标点与Markdown不重叠）
A ∩ M = ∅  （标注与Markdown不重叠）
```

### 3.2 符号选择策略

#### 策略1：标点符号优先使用Unicode CJK区

**原则**：
- ✅ 使用U+3000-U+303F（CJK Punctuation）
- ✅ 使用U+FF00-U+FFEF（Fullwidth Forms）
- ✅ 使用U+2000-U+206F（General Punctuation，弯引号等）
- ❌ 禁止使用ASCII范围（U+0020-U+007F）

**理由**：
- ASCII符号大量用于Markdown语法（`#` `*` `-` `[` `]` `` ` ``等）
- CJK全角符号不会与Markdown冲突
- 弯引号（" " ' '）是现代中文标点规范用法

#### 策略2：标注符号使用稀有Unicode括号

**原则**：
- ✅ 实体标注使用`〖〗`（U+3016/U+3017，白色六角括号）— 语义层：世界中的对象
- ✅ 修辞标注使用`〘〙`（U+3018/U+3019，白色龟甲括号）— 语言层：文本自身的语言特征
- ✅ 动词标注使用`⟦⟧`（U+27E6/U+27E7，数学白方括号）— 事件层：世界中的行为
- ❌ 禁止使用常见括号：`()` `[]` `{}` `（）` `【】`

**理由**：
- `[]` 用于Markdown链接和段落编号
- `{}` 用于某些Markdown扩展语法
- `（）` 可能在原文中出现（注释用法）
- 稀有Unicode括号不会与原文或Markdown冲突

#### 策略3：类型标记符号避开标点和Markdown（⚠️ 当前版本未完全遵守）

**当前状态**：
- ✅ 使用ASCII符号：`@ = ; # % & ' ^ ~ • ! ? + { : [ _ $`
- ✅ 在`〖〗`或`⟦⟧`内部，被括号保护，Markdown解析器不会识别
- ⚠️ **但部分符号与Markdown有交集**：`# _ [ $ : { ~ |`（详见§2.2.1）

**当前可用性**：
- 短期：✅ 可用（括号提供保护，无实际解析冲突）
- 长期：⚠️ 建议修正（影响可读性，不符合严格的Disjoint原则）

**理由（为何当前设计可用但不理想）**：
- ✅ **技术可行**：符号在`〖〗`内部时，Markdown解析器不会识别
- ✅ **输入方便**：ASCII符号易于手工标注
- ⚠️ **可读性差**：`〖#身份〗` `〖_思想〗` `〖[刑法〗` 在阅读时容易与Markdown混淆
- ⚠️ **维护性弱**：未来如果更换渲染器，可能产生新的冲突

**未来改进方向**：
- 用独特的Unicode符号替换有冲突的类型标记
- 优先替换：`# _ [` （高频Markdown符号）
- 低优先级：`$ : { ~ |`（低频或扩展语法）

### 3.3 当前符号冲突分析

#### 冲突1：`$`符号（数量标记 vs Markdown数学公式）

**现状**：
- 标注：`〖$三万人〗`
- Markdown数学：`$E=mc^2$`（某些Markdown渲染器支持）

**分析**：
- ✅ **无冲突**：`$`在`〖〗`内部，Markdown解析器不会识别为数学公式
- ✅ **可区分**：`〖$`是固定模式，`$`单独出现才是数学公式

**结论**：当前设计安全，无需修改。

#### 冲突2：`#`符号（身份标记 vs Markdown标题）

**现状**：
- 标注：`〖#天子〗`
- Markdown标题：`# 标题`

**分析**：
- ✅ **无冲突**：`〖#`是固定模式，在括号内部
- ✅ **可区分**：Markdown标题的`#`在行首，且后面有空格

**结论**：当前设计安全，无需修改。

#### 冲突3：`|`符号（消歧分隔 vs Markdown表格）

**现状**：
- 标注：`〖@台|吕台〗`
- Markdown表格：`| 列1 | 列2 |`

**分析**：
- ✅ **无冲突**：`|`在`〖〗`内部，不在行首
- ✅ **可区分**：Markdown表格的`|`在行首或跨多列

**结论**：当前设计安全，无需修改。

#### 冲突4：`[`符号（刑法标记 vs Markdown链接/段落编号）

**现状**：
- 标注：`〖[族诛〗`
- Markdown段落编号：`[1]` `[1.1]`
- Markdown链接：`[文字](url)`

**分析**：
- ✅ **无冲突**：`〖[`是固定模式，与`[`单独出现不同
- ⚠️ **潜在混淆**：人工阅读时，`[`出现频率高，可能混淆

**结论**：当前设计可用，但需要在lint脚本中明确检测规则。

#### 冲突5：`{`符号（典籍标记 vs Markdown部分扩展）

**现状**：
- 标注：`〖{春秋〗`
- Markdown某些扩展：`{.class}` `{#id}`（属性语法）

**分析**：
- ✅ **无冲突**：`〖{`是固定模式
- ✅ **可区分**：Markdown属性语法的`{`单独出现

**结论**：当前设计安全，无需修改。

### 3.4 符号冲突总结

**当前状态评估**：

| 维度 | 评估 | 说明 |
|-----|------|------|
| **技术可行性** | ✅ 可用 | 括号保护机制有效，无实际解析冲突 |
| **严格Disjoint** | ⚠️ 部分违反 | 8个类型标记与Markdown有交集（`# _ [ $ : { ~ |`） |
| **可读性** | ⚠️ 不理想 | `〖#身份〗` `〖_思想〗` 等容易与Markdown混淆 |
| **维护性** | ⚠️ 有风险 | 未来渲染器变更可能产生新问题 |

**关键设计决策（当前版本）**：
1. ✅ **标点符号全部使用CJK全角区**，避开ASCII → **完全Disjoint**
2. ✅ **标注符号使用稀有Unicode括号** `〖〗` `⟦⟧` → **完全Disjoint**
3. ⚠️ **类型标记符号在括号内部**，依赖括号保护 → **技术可用，但非严格Disjoint**

**需要注意的边界情况**：
- Markdown数学公式（`$...$`）：与`〖$数量〗`在括号外可能冲突
- Markdown表格（`|`）：与`〖TYPE 名|规范名〗`中的消歧符可能混淆
- 段落编号（`[数字]`）：与`〖[刑法〗`中的`[`可能视觉混淆
- Markdown标题（`# 标题`）：与`〖#身份〗`在行首可能混淆

**⚠️ 未来改进计划（低优先级）**：

| 阶段 | 工作 | 优先级 |
|-----|------|--------|
| **Phase 1** | 替换高频冲突符号：`# _ [` | P2（中） |
| **Phase 2** | 替换中频冲突符号：`$ : {` | P3（低） |
| **Phase 3** | 替换低频冲突符号：`~ |` | P4（极低） |

### 3.5 替代符号方案（100个候选Unicode符号）

**设计原则**：
- ✅ **视觉独特**：不与标点、Markdown冲突
- ✅ **语义关联**：符号形状与实体类型有直观联系
- ✅ **输入可行**：可通过Unicode输入法或快捷键输入
- ✅ **渲染兼容**：主流字体（Noto Sans、Source Han Sans）支持

#### 3.5.1 优先推荐（8个核心替换）

当前有冲突的8个符号的推荐替代方案：

| 当前符号 | 类型 | 推荐替代 | Unicode | 语义联系 | 优先级 |
|---------|------|---------|---------|---------|--------|
| `#` | 身份 | `◎` | U+25CE | 双圆圈，象征"身份地位" | P2 高 |
| `_` | 思想 | `⊙` | U+2299 | 有点圆圈，象征"思想核心" | P2 高 |
| `[` | 刑法 | `⊡` | U+22A1 | 方框加点，象征"律法条目" | P2 高 |
| `$` | 数量 | `⊕` | U+2295 | 加号圆圈，象征"数量累加" | P3 中 |
| `:` | 礼仪 | `◉` | U+25C9 | 鱼眼圆圈，象征"礼仪庄重" | P3 中 |
| `{` | 典籍 | `◈` | U+25C8 | 白色菱形，象征"典籍珍贵" | P3 中 |
| `~` | 族群 | `◐` | U+25D0 | 半黑圆，象征"族群融合" | P4 低 |
| `|` | 消歧符 | `⊚` | U+229A | 环形点，视觉分隔性强 | P4 低 |

**⚠️ 视觉混淆风险**：
- `◆` (U+25C6, 黑菱形) - 实体标注：邦国
- `◈` (U+25C8, 白菱形含点) - 动词标注：军事
- 两者在快速阅读时容易混淆，建议未来替换邦国符号为视觉区分度更高的符号

#### 3.5.2 完整候选符号库（100个）

**几何图形类（30个）**：

| 符号 | Unicode | 名称 | 适用类型建议 |
|-----|---------|------|------------|
| `○` | U+25CB | 白圆 | 可读性强，通用 |
| `◎` | U+25CE | 靶心圆 | **身份**（推荐） |
| `◉` | U+25C9 | 鱼眼 | **礼仪**（推荐） |
| `◌` | U+25CC | 虚圆 | - |
| `◍` | U+25CD | 半黑圆（右） | - |
| `◐` | U+25D0 | 半黑圆（左右） | **族群**（推荐） |
| `◑` | U+25D1 | 半黑圆（右左） | - |
| `◒` | U+25D2 | 半黑圆（下上） | - |
| `◓` | U+25D3 | 半黑圆（上下） | - |
| `◔` | U+25D4 | 四分之一黑圆（右上） | - |
| `◕` | U+25D5 | 四分之一黑圆（右下） | - |
| `◖` | U+25D6 | 半黑圆（左） | - |
| `◗` | U+25D7 | 半黑圆（右） | - |
| `◘` | U+25D8 | 反白方框 | - |
| `◙` | U+25D9 | 反白方框（小点） | - |
| `◚` | U+25DA | 半黑方框（上） | - |
| `◛` | U+25DB | 半黑方框（右下） | - |
| `◜` | U+25DC | 弧形（左上） | - |
| `◝` | U+25DD | 弧形（右上） | - |
| `◞` | U+25DE | 弧形（右下） | - |
| `◟` | U+25DF | 弧形（左下） | - |
| `◠` | U+25E0 | 上半圆弧 | - |
| `◡` | U+25E1 | 下半圆弧 | - |
| `◢` | U+25E2 | 黑三角（右下） | - |
| `◣` | U+25E3 | 黑三角（左下） | - |
| `◤` | U+25E4 | 黑三角（左上） | - |
| `◥` | U+25E5 | 黑三角（右上） | - |
| `◦` | U+25E6 | 白色小圆 | - |
| `◧` | U+25E7 | 方框（下半白） | - |
| `◨` | U+25E8 | 方框（右半白） | - |

**数学运算符类（30个）**：

| 符号 | Unicode | 名称 | 适用类型建议 |
|-----|---------|------|------------|
| `⊕` | U+2295 | 圆加号 | **数量**（推荐） |
| `⊖` | U+2296 | 圆减号 | - |
| `⊗` | U+2297 | 圆乘号 | - |
| `⊘` | U+2298 | 圆斜线 | - |
| `⊙` | U+2299 | 圆点 | **思想**（推荐） |
| `⊚` | U+229A | 环形点 | **消歧符**（推荐） |
| `⊛` | U+229B | 星号圆 | - |
| `⊜` | U+229C | 圆等号 | - |
| `⊝` | U+229D | 圆减（短） | - |
| `⊞` | U+229E | 方加号 | - |
| `⊟` | U+229F | 方减号 | - |
| `⊠` | U+22A0 | 方乘号 | - |
| `⊡` | U+22A1 | 方点 | **刑法**（推荐） |
| `⊢` | U+22A2 | 推出符 | - |
| `⊣` | U+22A3 | 推入符 | - |
| `⊤` | U+22A4 | T形符 | - |
| `⊥` | U+22A5 | 底部符 | - |
| `⊦` | U+22A6 | 断言符 | - |
| `⊧` | U+22A7 | 模型符 | - |
| `⊨` | U+22A8 | 真符 | - |
| `⊩` | U+22A9 | 强制符 | - |
| `⊪` | U+22AA | 三竖线 | - |
| `⊫` | U+22AB | 双竖线 | - |
| `⊬` | U+22AC | 非推出 | - |
| `⊭` | U+22AD | 非真 | - |
| `⊮` | U+22AE | 非强制 | - |
| `⊯` | U+22AF | 非双推出 | - |
| `⊰` | U+22B0 | 前驱符 | - |
| `⊱` | U+22B1 | 后继符 | - |
| `⊲` | U+22B2 | 左三角 | - |

**星形与特殊符号类（20个）**：

| 符号 | Unicode | 名称 | 适用类型建议 |
|-----|---------|------|------------|
| `★` | U+2605 | 黑星 | 天文 |
| `☆` | U+2606 | 白星 | 天文 |
| `✦` | U+2726 | 四角星 | - |
| `✧` | U+2727 | 四角白星 | - |
| `✪` | U+272A | 圆内星 | - |
| `✫` | U+272B | 开放星 | - |
| `✬` | U+272C | 黑心星 | - |
| `✭` | U+272D | 重影星 | - |
| `✮` | U+272E | 强调星 | - |
| `✯` | U+272F | 风车星 | - |
| `✰` | U+2730 | 影子星 | - |
| `✱` | U+2731 | 重星 | - |
| `✲` | U+2732 | 开放中心星 | - |
| `✳` | U+2733 | 八角星 | - |
| `✴` | U+2734 | 八角黑星 | - |
| `✵` | U+2735 | 八瓣花星 | - |
| `✶` | U+2736 | 六瓣黑星 | - |
| `✷` | U+2737 | 八瓣白星 | - |
| `✸` | U+2738 | 强调花星 | - |
| `✹` | U+2739 | 气球花星 | - |

**箭头与指示符类（10个）**：

| 符号 | Unicode | 名称 | 适用类型建议 |
|-----|---------|------|------------|
| `➊` | U+277A | 圆圈数字1 | 可用于顺序 |
| `➋` | U+277B | 圆圈数字2 | - |
| `➌` | U+277C | 圆圈数字3 | - |
| `➍` | U+277D | 圆圈数字4 | - |
| `➎` | U+277E | 圆圈数字5 | - |
| `➏` | U+277F | 圆圈数字6 | - |
| `➐` | U+2780 | 圆圈数字7 | - |
| `➑` | U+2781 | 圆圈数字8 | - |
| `➒` | U+2782 | 圆圈数字9 | - |
| `➓` | U+2783 | 圆圈数字10 | - |

**杂项符号类（10个）**：

| 符号 | Unicode | 名称 | 适用类型建议 |
|-----|---------|------|------------|
| `◈` | U+25C8 | 白色菱形 | **典籍**（候选，注意与动词冲突） |
| `◆` | U+25C6 | 黑菱形 | - |
| `◇` | U+25C7 | 白菱形 | 当前用于动词（经济） |
| `◊` | U+25CA | 菱形 | - |
| `♦` | U+2666 | 实心菱形 | - |
| `♢` | U+2662 | 空心菱形 | - |
| `⬟` | U+2B1F | 五边形 | - |
| `⬠` | U+2B20 | 白五边形 | - |
| `⬡` | U+2B21 | 白六边形 | - |
| `⬢` | U+2B22 | 黑六边形 | - |

#### 3.5.3 符号选择指南

**选择标准**（按优先级排序）：

1. **视觉区分度** ✅ 必须
   - 与现有符号 `@ = ; % & ' ^ ~ • ! ? +` 明显不同
   - 与标点符号 `。，、；：？！" "` 区分清晰
   - 与Markdown符号 `# * - [ ] \` ` 无混淆

2. **语义关联性** ⚠️ 建议
   - 身份 `#` → `◎`：圆形层次，象征社会地位
   - 思想 `_` → `⊙`：中心点，象征思想核心
   - 刑法 `[` → `⊡`：方框约束，象征法律规范
   - 数量 `$` → `⊕`：加号运算，象征数量累加

3. **字体兼容性** ✅ 必须
   - 测试主流字体：Noto Sans CJK、Source Han Sans、思源黑体
   - 确保Web渲染和PDF导出正常显示

4. **输入便捷性** ⚠️ 考虑
   - 可通过Unicode输入法输入（如 `U+25CE`）
   - 或配置VSCode快捷键（如 `Ctrl+Alt+1` → `◎`）

#### 3.5.4 迁移实施方案

**当决定替换时的实施步骤**：

```bash
# 阶段1：符号决策（需人工审查）
# 1. 从100个候选中选择8个最终符号
# 2. 更新 SKILL_03a 和 SKILL_01g 文档
# 3. 创建迁移映射表 symbol_migration_map.json

# 阶段2：代码迁移（自动化）
python scripts/migrate_type_symbols.py \
  --mapping symbol_migration_map.json \
  --dry-run  # 先预览

# 阶段3：批量替换（自动化）
python scripts/migrate_type_symbols.py \
  --mapping symbol_migration_map.json \
  --execute  # 执行替换

# 阶段4：验证（自动化+人工）
python scripts/lint_symbol_conflicts.py chapter_md/*.md
python scripts/lint_text_integrity.py chapter_md/*.md

# 阶段5：文档同步
# 更新所有相关SKILL文档中的示例
```

**迁移脚本规范**（`scripts/migrate_type_symbols.py`，待开发）：
- 输入：`symbol_migration_map.json`（旧符号→新符号映射）
- 输出：所有 `chapter_md/*.tagged.md` 文件
- 验证：替换后运行完整性检查，确保原文未被修改
- 回滚：保留备份文件，支持一键回滚

---

## 四、符号使用禁忌

### 4.1 禁止混用的符号

| 禁止情况 | 说明 | 正确做法 |
|---------|------|---------|
| ❌ 半角标点 | 引号`"` `'`、逗号`,`、句号`.` | 使用全角：" " ' '，。 |
| ❌ 直角引号 | 「」『』 | 使用弯引号：" " ' ' |
| ❌ 标点在标注内 | `〖@项羽，〗` | 标点在外：`〖@项羽〗，` |
| ❌ Markdown在标注内 | `〖@**项羽**〗` | 标注在外：`**〖@项羽〗**` |
| ❌ 嵌套标注 | `〖#〖@太子〗〗` | 选择一个类型：`〖#太子〗` |
| ❌ 标注跨标点 | `〖@项羽，字〗` | 分开标注：`〖@项羽〗，字` |

### 4.2 符号层次规则

**正确的嵌套层次**（由外到内）：
```
Markdown外层 > 标注符号 > 标点符号 > 原文内容
```

**示例**：
```markdown
✅ 正确层次：
**〖@项羽〗曰："〖@沛公〗安在？"**
└─Markdown┘ └标注┘ └标点┘ └标注┘  └标点┘

❌ 错误层次1：
〖@**项羽**〗
（Markdown在标注内部，违反层次）

❌ 错误层次2：
"〖@项羽曰〗"
（标点包裹标注，应该是标注包裹原文）
```

---

## 五、符号冲突检测方法

### 5.1 检测逻辑

符号冲突检测需要验证以下6个方面：

#### 检测1：禁止半角标点
- **检查逻辑**：在移除标注符号和段落编号后，查找文本中的半角标点 `,.:;?!"\' `
- **注意**：需要排除标注内部的类型标记符号（如 `〖@name〗` 中的 `@`）
- **排除**：段落编号 `[N.N]` 中的句点、Markdown自定义块 `:::` 中的冒号

#### 检测2：禁止直角引号
- **检查逻辑**：查找日文风格的直角引号 `「」『』`
- **修正建议**：改为弯引号 `" " ' '`

#### 检测3：标点在标注内部
- **检查逻辑**：在 `〖TYPE content〗` 模式中，检查 `content` 是否包含标点符号（除消歧符 `|`）
- **正确做法**：标点应在标注外部，如 `〖@项羽〗，〖@刘邦〗`
- **错误示例**：`〖@项羽，刘邦〗`

#### 检测4：Markdown在标注内部
- **检查逻辑**：在 `〖TYPE content〗` 模式中，检查 `content` 是否包含Markdown符号 `* _ ` [ ] >`
- **层次原则**：Markdown外层 > 标注符号 > 原文内容

#### 检测5：嵌套标注
- **检查逻辑**：检测 `〖TYPE1 ... 〖TYPE2 ... 〗 ... 〗` 模式
- **修正策略**：拍平或选择最合适的类型标记
- **已有工具**：`lint_text_integrity.py --check-nested` 已实现此功能

#### 检测6：标注跨越标点
- **检查逻辑**：在 `〖TYPE content〗` 中，检查 `content` 是否包含句子边界标点 `。！？`
- **错误示例**：`〖@项羽曰。〗` 应改为 `〖@项羽〗曰。`

### 5.2 脚本规划

**脚本位置**：`scripts/lint_symbol_conflicts.py` ✅ 已实现

**调用方式**：
```bash
# 检查所有章节
python scripts/lint_symbol_conflicts.py chapter_md/*.md

# 生成报告到文件
python scripts/lint_symbol_conflicts.py chapter_md/*.md --report logs/symbol_conflicts.txt

# 只检查特定类型
python scripts/lint_symbol_conflicts.py chapter_md/*.md --check-types halfwidth,nested
```

**输出示例**：
```
【符号冲突检查报告】
检查文件: 130 个
发现问题: 10816 处

问题分类:
  - 半角标点: 10500 处（主要是直引号 " 应改为 " "）
  - 嵌套标注: 253 处
  - 直角引号: 57 处（「」『』 应改为 " " ' '）
  - Markdown在标注内: 3 处
  - 标点在标注内: 2 处
  - 标注跨标点: 1 处
```

### 5.3 与现有脚本的关系

**lint_text_integrity.py**：
- 功能：检查标注完整性（原文字符未被修改）
- 包含：嵌套标注检测（`--check-nested`）
- 关系：`lint_symbol_conflicts.py` 专注符号冲突，是完整性检查的补充

**建议检查流程**：
```bash
# 完整性检查管线（按顺序执行）
python scripts/lint_text_integrity.py chapter_md/*.md --check-nested  # 步骤1: 文本完整性
python scripts/lint_symbol_conflicts.py chapter_md/*.md               # 步骤2: 符号冲突
```

---

## 六、检查清单

### 执行前检查

- [ ] 确认使用全角标点（无半角`,` `.` `;`等）
- [ ] 确认使用弯引号" " ' '（无直角引号「」『』）
- [ ] 确认换行符为LF（无CRLF）

### 执行中验证

- [ ] **半角标点检查**：无半角标点符号
- [ ] **直角引号检查**：无日文引号「」『』
- [ ] **标点位置检查**：标点在标注外部，不在内部
- [ ] **Markdown层次检查**：Markdown在标注外部
- [ ] **嵌套标注检查**：无嵌套标注〖#〖@...〗〗
- [ ] **跨标点标注检查**：标注不跨越句子边界

### 执行后验证

- [ ] **符号冲突检查通过**：`lint_symbol_conflicts.py` 无错误
- [ ] **完整性检查通过**：`lint_text_integrity.py` 无错误
- [ ] **人工抽查**：随机抽查10处标注，确认符号使用正确

---

## 七、FAQ

### Q1: 为什么`$`符号可以用于数量标记，不会与Markdown数学公式冲突？

**A**: 因为`$`在`〖$数量〗`中是固定模式，Markdown解析器会识别：
- `〖$三万人〗` → 整体不是数学公式（`〖`开头）
- `$E=mc^2$` → 明确的数学公式（单独的`$`包裹）

**关键**：`〖$`是固定前缀，与`$`单独出现不同。

### Q2: 标点符号可以在标注内部吗？

**A**: **不可以**。标点是原文的一部分，标注是语义标记：

❌ **错误**：`〖@项羽，字籍〗`
✅ **正确**：`〖@项羽〗，字〖@籍〗`

**原因**：
- 标点属于原文，标注去除后应保留标点
- 标点在内部会导致显示时标点被包裹在标注样式中

### Q3: Markdown粗体可以包裹标注吗？

**A**: **可以**。Markdown在标注外层：

✅ **正确**：`**〖@项羽〗**`（粗体包裹标注）
❌ **错误**：`〖@**项羽**〗`（标注包裹Markdown）

**原则**：Markdown外层 > 标注符号 > 原文内容

### Q4: 段落编号`[1]`会与刑法标记`〖[族诛〗`冲突吗？

**A**: **不会冲突**，但需要明确规范：

- **段落编号**：`[数字]`，单独出现，不参与实体标注
- **刑法标记**：`〖[刑法名〗`，有`〖`前缀

**lint脚本**：可以区分这两种模式。

### Q5: 如何处理原文中已有的括号（如注释）？

**A**: 原文括号保持不变，不与标注符号冲突：

- **原文括号**：`（注释内容）`（全角圆括号）
- **标注括号**：`〖TYPE 内容〗`（白色六角括号）

**关键**：使用不同的Unicode字符，完全disjoint。

---

## 八、相关文档

### 关联Skill
- [SKILL_01f](SKILL_01f_句读和标点校勘.md) - 句读和标点校勘
- [SKILL_01a](SKILL_01a_标注完整性维护.md) - 标注完整性维护
- [SKILL_03a](SKILL_03a_实体标注.md) - 实体标注规范

### 参考标准
- 《标点符号用法》国家标准（GB/T 15834-2011）
- Unicode标准：CJK Punctuation区（U+3000-U+303F）
- Markdown规范：CommonMark Specification

---

**维护者**: Claude + 用户
**创建日期**: 2026-04-01
**最后更新**: 2026-04-01
**版本**: v1.0
