---
name: skill-03b
description: 古籍人名消歧方法,区分同名异人与异名同人。当发现'李斯(秦相)'与'李斯(他人)'等同名冲突时使用。涵盖规范名设计、消歧语法(〖@显示名|规范名〗)、别名 4 列结构、全局一致性检查等内容。
---


# SKILL 03b: 人名消歧 — 从短称到唯一指称

> 从《史记》130篇 **4,921 个人名**的消歧实践中提炼，适用于古籍中同名异指的自动化解决。
>
> 最新进展（2026-04-19）：`entity_aliases.json` 升级为 **4 列结构**（surface / type / canonical / refs），人名别名从旧版 591 条扩充到 **3,322 条**（覆盖 2,137 个短名 → 2,097 个 canonical，其中 **550 个短名存在歧义**）。相关抽取脚本见 §十一。

---

## 一、问题定义

### 1.1 为什么需要消歧

古文中人物常以短称出现：谥号（"武王"）、爵号（"桓公"）、通称（"太子"）。同一短称在不同上下文可指不同人物：

| 短称 | 周本纪 | 秦本纪 | 楚世家 |
|------|--------|--------|--------|
| 武王 | 周武王 | 秦武王 | 楚武王 |
| 昭王 | 周昭王 | 秦昭王 | 楚昭王 |
| 文公 | — | 秦文公 | — |
| 桓公 | — | 秦桓公 | — |

**数据规模（2026-04-19）**：

| 指标 | 数值 | 备注 |
|------|-----:|------|
| 人名实体总数 | **4,921** | `entity_index.json` 的 person 条目 |
| 全部实体总数 | ~15,120 | 名词 18 类合计 |
| 歧义短名 | **550** | `entity_aliases.json` 中同一 surface 对应多 canonical |
| 别名条目（4 列）| **3,322** | `entity_aliases.json`（person 分片）|
| 唯一 canonical | 2,097 | 已消歧的规范人名 |
| 章节级映射 | 644 | `disambiguation_map.json`，覆盖 71 章 |
| 无需消歧章节 | 53 | 无歧义短名出现 |
| 同章多指代局限 | 6 | 见 §7.2 |

**歧义分布（surface → canonical 数）**：1→1587 条；2→321；3→89；4→50；5→31；6→19；7→12；8→11；9→4；10→4；≥11→9。最严重者见 §2.2。

### 1.2 核心约束

**两种消歧方式**（可混用）：

**方式A：外部JSON消歧（章节级，不改源文件）**
```
源文件(.tagged.md)  →  不改动，〖@武王〗保持原样
消歧元数据(.json)   →  {"004": {"武王": "周武王"}}
渲染HTML            →  <span title="人名：周武王">武王</span>
```

**方式B：内联消歧（v2.7，适用所有13种〖TYPE〗实体类型）**
```
源文件(.tagged.md)  →  〖@台|吕台〗（人名：显示"台"，全名"吕台"）
                    →  〖=函谷|函谷关〗（地名：显示"函谷"，全名"函谷关"）
                    →  〖◆汉|大汉〗（邦国：显示"汉"，全名"大汉"）
无需JSON            →  内联 | 后的规范名优先级高于JSON消歧
渲染HTML            →  <span class="TYPE" title="类型：规范名" data-canonical="规范名">显示名</span>
```

**各类型典型使用场景**：
| 类型 | 消歧场景 | 示例 |
|------|---------|------|
| 人名 | 单字省称、同名消歧 | `〖@台\|吕台〗`、`〖@桓公\|齐桓公〗` |
| 地名 | 地名简称、多义地名 | `〖=函谷\|函谷关〗`、`〖=沛\|沛县〗` |
| 官职 | 同类官职区分 | `〖;守\|郡守〗`、`〖;将\|上将军〗` |
| 邦国 | 同名邦国（汉/楚多指） | `〖◆汉\|汉王国〗`、`〖◆楚\|楚国〗` |
| 氏族 | 姓/氏规范化 | `〖&芈\|芈姓〗`、`〖&嬴\|嬴姓〗` |
| 时间 | 纪年省称 | `〖%元\|元年〗` |
| 制度 | 制度全称 | `〖^郡县\|郡县制〗` |
| 族群 | 族群全称 | `〖~胡\|匈奴〗` |

**适用场景对比**：
| | JSON消歧 | 内联消歧 `〖TYPE X\|Y〗` |
|--|---------|-------------------|
| 优点 | 不改源文件，集中管理 | 标注即消歧，无需维护JSON |
| 缺点 | 需维护disambiguation_map.json | 标注文件稍冗长 |
| 适用 | 章内一义的谥号（武王→周武王） | 单字省称（台→吕台）、非人名消歧 |

---

## 二、歧义短名的类型学

### 2.1 三层歧义结构

```
第一层：谥号歧义（最常见，81个）
  "武王" → 周武王 / 秦武王 / 楚武王
  "桓公" → 齐桓公 / 宋桓公 / 晋桓公 / 郑桓公 / ...
  "惠王" → 秦惠王 / 魏惠王 / 燕惠王 / 周惠王

第二层：同国同谥号歧义（罕见但存在）
  齐"桓公" → 齐桓公（小白）vs 齐桓侯（午）
  晋"献公" → 晋献公（诡诸）vs 历史上其他晋献公

第三层：通称歧义（如"太子"、"王"、"公子"）
  → 不在当前消歧范围内（需段落级而非章节级处理）
```

**谥号识别的系统方法**：关于谥号字符集、爵位等级、前缀验证规则等详细逻辑，参见 [doc/spec/PLAN_谥号索引需求.md § 谥号识别逻辑](../doc/spec/PLAN_谥号索引需求.md#谥号识别逻辑)。该文档定义了：
- 58个谥号字符（美谥/平谥/恶谥）
- 七等爵位系统（帝>王>公>侯>伯>子>男）
- 不同爵位的前缀验证规则
- 排除规则（思想家称号、封地侯）
- 章节权重分级（本纪/世家/年表 vs 普通章节）

### 2.2 歧义最严重的短名（2026-04-19 实测，跨章节指代不同人物）

从 `entity_aliases.json` 的 4 列结构中按 `surface → unique canonicals` 数量排序，Top 15：

| 短名 | canonical 数 | 代表消歧候选（前几条）|
|------|:---:|---------------------|
| 武公 | 14 | 卫/周/宋/晋/曹/杞/陈/秦/鲁/赵/齐/宋前武公/武公寿/晋前武公 |
| 成 | 13 | 公子成、即墨成、周成王、大骆子成、宁成、宋成、刘成… |
| 哀王 | 12 | 中山/临江/山阳/周哀王、楚哀王、齐哀王、刘襄… |
| 婴 | 12 | 夏侯婴、子婴、晏婴、灌婴、田婴、程婴… |
| 悼公 | 12 | 卫/宋/晋/杞/陈/齐悼公、悼公成、悼公购由… |
| 桓公 | 12 | 卫/姜齐/宋/晋/杞/燕桓公 等 |
| 胜 | 12 | 刘胜、平原君、张胜、楚隐王、武公、田胜… |
| 信 | 11 | 刘信、卫信、成信、李信、王信、田信… |
| 武 | 11 | 刘武、周武王、张武、楚武王、武王、蒙武… |
| 光 | 10 | 公子光、公孙光、刘光、务光、吴公子光、吴王光… |
| 共公 | 10 | 共公襄、厉共公、宋/曹/杞/燕共公… |
| 惠王 | 10 | 刘武、周/广川/梁/楚惠王、惠文君… |
| 文公 | 10 | 卫/周/晋/杞/燕/秦文公… |
| 仲 | 9 | 刘仲、张仲、彭越、王仲、秦仲、简王… |
| 共王 | 9 | 代/楚/周/城阳/成阳/梁/郑共王、刘喜… |

中歧义（4-8 种指代）：约 123 个短名（占全部歧义短名的 22%）。
轻歧义（2-3 种指代）：约 410 个短名。
完整清单见 `kg/entities/data/alias_conflicts.json` 的 `ambiguous_surfaces` 字段（585 条）。

### 2.3 不需消歧的名称（SKIP_NAMES）

以下名称虽然表面上像短称，但在史记中指代唯一，不应进入消歧流程：

| 类别 | 示例 | 原因 |
|------|------|------|
| 帝号 | 孝文帝、孝景帝、孝武帝 | 汉代帝号在史记中无歧义 |
| 上古人物 | 黄帝、炎帝 | 传说人物，唯一指代 |
| 已含国名的全称 | 齐桓公、秦昭王、晋文公 | 已经消歧，不需处理 |
| 汉代封号 | 梁孝王、淮南王、平津侯 | 史记中唯一指代 |
| 通称性短名 | 太后、太公、周公 | 语义过于宽泛，章节级消歧不可靠 |
| 带前缀的复合谥号 | 惠文王、武灵王、庄襄王 | 已含国名信息，唯一指代 |

---

## 三、四层启发式消歧策略

### 3.1 策略优先级

```
优先级1：前缀国名（最精确，前10字符内有&国名&）
  "&秦&@昭王@" → 秦昭王（置信度：极高）

优先级2：附近国名（80字窗口内的&国名&或@国名X@）
  "...&楚&...诸事...@怀王@..." → 楚怀王（置信度：高）

优先级3：章节主题国（由CHAPTER_STATE表提供）
  周本纪(004)中的"武王" → 默认周武王（置信度：中高）

优先级4：共现全名（同章出现@秦昭王@和@昭王@）
  → "昭王"指秦昭王（置信度：中）
```

### 3.2 策略1：前缀国名

```python
def find_preceding_state_prefix(content, pos):
    """检查@tag@前10字符内是否有&state&"""
    pre_context = content[max(0, pos - 10):pos]
    m = re.search(r'&([^&]+)&$', pre_context)
    if m and m.group(1) in ALL_STATES_SET:
        return m.group(1)
    return None
```

**示例**：`&秦&@昭王@十三年` → 前缀"秦" → 查RULER_DB → 秦昭王

**局限**：原文中国名前缀不总是紧邻短名（"秦之昭王"中间隔了"之"）。

### 3.3 策略2：附近国名

```python
def find_state_tags_nearby(content, pos, window=80):
    """在pos前后window字符内查找所有&国名&和@国名X@"""
    context = content[max(0, pos-window):min(len(content), pos+window)]
    # 查找 &国名& 标记
    for m in re.finditer(r'&([^&\n]+)&', context):
        if m.group(1) in ALL_STATES_SET:
            found.append(m.group(1))
    # 查找 @国名XX@ 标记中的国名前缀
    for m in re.finditer(r'@([^@\n]+)@', context):
        if person[0] in ALL_STATES_SET:
            found.append(person[0])
    return found
```

**窗口大小**：80字符（约2-3句古文）。太大则噪声增多，太小则覆盖不足。

**局限**：多国并提时可能选错（"秦昭王与楚怀王会于...@王@曰"中的"王"可能指任一方）。

### 3.4 策略3：章节主题国

每个章节有一个"主题国"（CHAPTER_STATE表），作为兜底消歧依据：

```python
CHAPTER_STATE = {
    '004': '周',   # 周本纪 → 默认周
    '005': '秦',   # 秦本纪 → 默认秦
    '032': '齐',   # 齐太公世家 → 默认齐
    '039': '晋',   # 晋世家 → 默认晋
    '040': '楚',   # 楚世家 → 默认楚
    '043': '赵',   # 赵世家 → 默认赵
    ...
}
```

**强信号章节**（主题国几乎涵盖所有短名）：
- 本纪（001-012）：周、秦、汉，短名几乎都指该朝人物
- 世家（031-060）：各诸侯国，短名基本指该国人物
- 列传（061-130）：混合度高，主题国信号较弱

**弱信号章节**（主题国不可靠）：
- 书（023-030）、表（013-022）：跨国综述
- 合传列传（如069苏秦、070张仪）：涉及多国

### 3.5 策略4：共现全名

```python
def find_cooccurring_fullnames(content, short_name):
    """在同一章节中查找包含short_name的全名@标记@"""
    pattern = r'@([^@\n]*' + re.escape(short_name) + r'[^@\n]*)@'
    # 如果只找到一个全名候选，且该全名的国名前缀在附近出现 → 采用
```

**示例**：章节中同时出现 `@秦昭王@` 和 `@昭王@` → "昭王"指秦昭王。

**局限**：如果章节中出现多个含"昭王"的全名（如 `@秦昭王@` 和 `@燕昭王@`），则无法仅凭共现消歧，需结合附近国名。

### 3.6 多数投票与冲突处理

同一章节同一短名可能出现多次，每次可能由不同策略消歧到不同人物。处理方法：

```python
# 收集每个(章节, 短名)的所有消歧结果
results = [("秦昭王", "nearby_state"), ("秦昭王", "chapter_state"), ...]

# 多数投票：≥2/3一致 → 采用
if top_count * 3 >= total * 2:
    chapter_map[chapter_id][short_name] = top_name
else:
    # 票数分裂 → 跳过，不消歧（保守原则）
    pass
```

---

## 四、已知错误模式与手动修正

### 模式1：附近国名误导

问题：nearby_state窗口内捕获到的国名不是短名所指的国。

```
章004（周本纪）：
  "...&燕&师伐...@惠王@..." → nearby_state误判为燕惠王
  实际应为：周惠王（周本纪中的惠王）
```

修正：MANUAL_CORRECTIONS 表覆盖。

### 模式2：多国并提段落

问题：段落同时讨论多个国家的事务，窗口内多国名共存。

```
章071（樗里子甘茂列传）：
  叙述秦楚交涉，&楚&频繁出现
  "@武王@" 被误判为楚武王，实为秦武王
```

修正：MANUAL_CORRECTIONS + 提升前缀国名策略的优先级。

### 模式3：主题国与短名不匹配

问题：章节主题国与短名的国籍不一致时，兜底策略失效。

```
章005（秦本纪）：
  "@惠王@" → chapter_state判为秦惠王
  但此处引述燕国事务，实为燕惠王
```

修正：当策略2（附近国名）与策略3（主题国）冲突时，应优先采用策略2。

### 模式4：同国多位同谥号君主

问题：齐国有齐桓公（小白，前685）和齐桓侯（午，前374），谥号相近。

当前处理：SKIP_NAMES中包含"齐桓侯"，避免将"桓公"错误映射；大多数章节中"桓公"默认指齐桓公（小白）。

### MANUAL_CORRECTIONS 表

对已知启发式错误的精确修正（优先级最高）：

```python
MANUAL_CORRECTIONS = {
    ('004', '元王'): '周元王',     # nearby_state:楚 误判
    ('004', '惠王'): '周惠王',     # nearby_state:燕 误判
    ('044', '惠王'): '魏惠王',     # nearby_state:秦 误判
    ('044', '襄王'): '魏襄王',     # nearby_state:秦 误判
    ('071', '武王'): '秦武王',     # nearby_state:楚 误判
    ('071', '昭王'): '秦昭王',     # nearby_state:楚 误判
    ('079', '昭王'): '秦昭王',     # nearby_state:周 误判
    ('087', '惠王'): '秦惠王',     # nearby_state:楚 误判
}
```

### 模式5：上古章节无消歧需求

五帝本纪(001)、夏本纪(002)等上古章节中，人名均为唯一指代（黄帝、颛顼、尧、舜等），不存在谥号歧义问题。消歧工作可跳过这些章节。

### 模式6：单字人名假阳性

"象""益""会""代""舍""段"等单字既是人名也是常见动词/副词/形容词。在古文中频繁产生假阳性标注。

```
已知假阳性：
- 001 [5]  @象@天 — "象"=效法，非舜弟象
- 001 [20] 皆@益@笃 — "益"=更加，非伯益
```

检测方法：上下文模式匹配（`以.{0,2}@象@天`、`皆@益@笃`、`日@益@`、`@益@盛/强/弱`）。

### 模式7：破损标记污染（已大部分修复）

原有655处破损@标记@，已通过 `fix_broken_tags.py` 五层策略自动修复331处，降至155处（76%↓）。

**五层修复策略**：
1. L1: `$@X@Y@$` → `$@XY@$`（封号+名字合并，167处）
2. L2: `@Title@KnownName@` → `@TitleKnownName@`（已知实体合并，52处）
3. L3: `$X@@Y@$` → `$X$@Y@$`（$/@混用修正，54处）
4. L4: 奇数@行三连@修复（27处）
5. L5: `$$Name@`/`弑其君Name@` 等常见误用（31处）

**剩余155处**：主要在世家章节（039/036/040/032等），偶数@行配对错位，需语义理解。
**脚本**：`kg/entities/scripts/fix_broken_tags.py`（--scan/--fix/--chapter/--summary）

### 模式8：引用性短名的默认消歧

列传和书/表中引述历史典故时使用的"武王""文王"等短名，绝大多数指周武王/周文王。可为非本纪/世家章节的这些短名设置默认映射：

| 短名 | 默认指代 | 适用场景 |
|------|---------|---------|
| 武王 | 周武王 | 非秦/楚本纪章节的引述 |
| 文王 | 周文王 | 非秦/楚本纪章节的引述 |
| 成王 | 周成王 | 非楚世家章节的引述 |

---

## 五、数据结构

### 5.1 输入

| 文件 | 位置 | 说明 |
|------|------|------|
| `*.tagged.md` | chapter_md/ | 130个标注文件，包含 `@人名@` 标记 |
| RULER_DB | 脚本内嵌 | 261条 (短名, 国名) → 全名 映射规则 |
| CHAPTER_STATE | 脚本内嵌 | 130章 → 主题国 映射 |
| SKIP_NAMES | 脚本内嵌 | 不参与消歧的名称集合 |
| MANUAL_CORRECTIONS | 脚本内嵌 | 手动修正表 |

### 5.2 输出

**disambiguation_map.json**（章节级消歧映射）：

```json
{
  "004": {
    "武王": "周武王",
    "成王": "周成王",
    "幽王": "周幽王",
    "文王": "周文王",
    ...
  },
  "005": {
    "昭王": "秦昭王",
    "惠王": "燕惠王",
    ...
  }
}
```

**语义**：在章节 `NNN` 中，所有 `@短名@` 的出现都映射到同一个全名。

**局限**：章节级消歧假设同一章节中同一短名只指代一个人。当同一章节中"昭王"在不同段落指不同人时（极罕见），需要升级到段落级消歧。

### 5.3 渲染集成

`render_shiji_html.py` 中的 `_add_entity_links()` 函数加载消歧映射：

```python
# 消歧流程（渲染时）
resolved = entity_text                    # "武王"
if css_class == 'person' and chapter_id:
    chapter_disambig = disambig_map.get(chapter_id, {})
    if entity_text in chapter_disambig:
        resolved = chapter_disambig[entity_text]  # "周武王"

# 别名解析 → 规范名
canonical = alias_map.get(resolved, resolved)  # 可能进一步合并

# 生成HTML：显示原文，tooltip显示全名
# <a href="person.html#entity-周武王" target="_blank">
#   <span class="person" title="人名：周武王">武王</span>
# </a>
```

---

## 六、Canonical命名规范

消歧后的全名通过别名系统（entity_aliases.json）进一步归一化到canonical name。

### 6.1 命名规则

| 类别 | Canonical模式 | 示例 | 别名示例 |
|------|---|---|---|
| 先秦诸侯 | 国+谥号 | 周武王, 秦昭王, 齐桓公 | 武王, 昭王, 桓公 |
| 秦始皇 | 秦始皇帝 | — | 始皇, 嬴政, 秦王政 |
| 汉帝 | 汉+通称 | 汉高祖, 汉文帝, 汉武帝 | 刘邦/沛公/高祖, 刘彻/武帝 |
| 诸侯王 | 封国+谥号 | 楚元王, 梁孝王 | — |
| 其他人物 | 最常用名 | 项羽, 商鞅, 韩信 | 项王/项籍, 卫鞅/商君 |

### 6.2 设计原则

1. **唯一性** — canonical name在整个知识库中无歧义
2. **源文忠实** — 优先使用史记自身的称呼习惯（汉帝用谥号而非个人名）
3. **个人名降为alias** — 刘邦/嬴政等作为别名存在，方便搜索但不作canonical
4. **旧canonical自动合并** — 如"厉王胡"→"周厉王"后，"厉王胡"加入别名列表

### 6.2a ⚠️ 禁止形式："谥号 + 本名"合并作 canonical

**🚨 铁律**：当消歧**本名**（`〖@名|canonical〗`）时，canonical **只能二选一**：

1. **纯谥号/封号**：`〖@辄|卫出公〗`（指向整个人）
2. **姓 + 名**：`〖@辄|姬辄〗`（A87 规范名优先本名原则）

**严禁"谥号 + 本名"合并形式**：

| ❌ 错（谥号+名） | ✅ 对（姓+名） |
|----------------|--------------|
| `〖@贷\|齐康公贷〗` | `〖@贷\|吕贷〗` |
| `〖@辟彊\|齐宣王辟彊〗` | `〖@辟彊\|田辟彊〗` |
| `〖@蒯聩\|卫庄公蒯聩〗` | `〖@蒯聩\|姬蒯聩〗` |
| `〖@郝\|楚哀王郝〗` | `〖@郝\|熊郝〗` |
| `〖@止\|晋烈公止〗` | `〖@止\|姬止〗` |
| `〖@旦\|周公旦〗` | `〖@旦\|姬旦〗` |
| `〖@政\|秦王政〗` | `〖@政\|嬴政〗` |
| `〖@迁\|赵王迁〗` | `〖@迁\|赵迁〗` |

**理由**：
- 谥号+名形式**信息冗余**（谥号已经唯一指向此人，加名等于重复编号）
- 不符合 `entity_index.json` 的规范命名约定（以姓+名为 alias 键）
- A87 明文规定"规范名优先本名（姓+名）"

**常见姓氏对照表**（《史记》主要邦国）：

| 邦国 | 姓氏 | canonical 本名样板 |
|------|------|------------------|
| 周/卫/晋/鲁/燕/郑/蔡/吴/曹/魏<sup>1</sup>/韩<sup>1</sup>/滕/虞/虢/中山 | 姬 | 姬旦（周公）/姬辄（卫出公）/姬夫差（吴王）|
| 楚 | 熊（芈姓熊氏）| 熊槐（楚怀王）/熊郝（楚哀王）|
| 齐 pre-田氏代齐（~386 BC 前） | 吕（姜姓吕氏）| 吕贷（齐康公）|
| 齐 post-田氏代齐 | 田（妫姓田氏）| 田辟彊（齐宣王）|
| 魏/韩<sup>1</sup> | 魏/韩（氏）| 魏斯（魏文侯）/韩虔（韩景侯）|
| 赵/代 | 赵（嬴姓赵氏）| 赵籍（赵烈侯）/赵嘉（代王嘉）|
| 秦 | 嬴 | 嬴政（秦王政）/嬴子楚（秦庄襄王）|
| 宋 | 子（子姓）| 子偃（宋康王）|
| 陈 | 妫（陈氏/田氏前身）| 妫午（陈成公）|
| 越 | 姒（姒姓）| 姒句践（越王句践）|
| 汉代刘姓封国（吴王濞/代王/梁王...）| 刘 | 刘濞（吴王濞）/刘恒（代王）|

<sup>1</sup> 注：春秋晋卿**赵/魏/韩** 以**氏名**作姓使用（如魏斯即魏氏之斯）；严格 `姓` 为姬姓，但《史记》惯称"魏斯"/"韩虔"。

**例外 1：同名异人不可区分 → 保留"爵号/封号 + 名"**

当姓+名会与另一知名人物撞名时，保留特殊爵号+名形式作为唯一指称（A87 "同名异人"条例已允许）：

| 保留形式 | 理由 |
|---------|------|
| `〖@信\|韩王信〗` | 姓+名=`韩信`，与淮阴侯韩信撞名（全库 ~37 处） |
| `〖@成\|韩王成〗` | 避免与其他"成"混淆（~5 处） |

这些是**明文特例**，全库扫描时需跳过，**不得机械替换**。

**例外 2：妃子 / 女性亲属 → 保留"夫君谥号 + 妃名"**

《史记》中多位妃子仅在"某王之妃"身份下被提及，缺乏独立家族姓氏系谱。此时 canonical 使用"夫君谥号 + 妃名"结构区分同名/通名女子：

| 形式 | 理由 |
|------|------|
| `〖@郑袖\|楚怀王郑袖〗` | 区分不同楚王的 郑 氏妃（可能多位） |
| `〖@越女\|楚庄王越女〗` / `〖@越女\|楚昭王越女〗` | "越女"是通名（越国嫁楚女），需指明夫君 |
| `〖@江羋\|楚成王江羋〗` / `〖@郑姬\|楚庄王郑姬〗` / `〖@巴姬\|楚共王巴姬〗` | 同上，国别+姓 类通名，需关系消歧 |

**判据**：surface 本身已是"国名+姓/女性号"复合形式（郑袖/越女/江羋/巴姬/郑姬），说明该女子无独立家族姓氏系谱可循。此时"姓+名"规则不适用，**允许保留关系式 canonical**。

**例外 3：身份复合名（公子/太子/王子/舍人）→ 保留原形**

`〖@丹|太子丹〗`、`〖@建|太子建〗`、`〖@胜|白公胜〗`、`〖@子闾|楚公子闾〗` 这类 canonical 由**身份词 + 名**组成，身份词（太子/公子/王子/白公/公）**不是谥号**，不在"谥号+名"禁止范围。这些是 A87 明文接受的消歧形式。

**易混点**：`太子 / 公子` 是身份词（〖#〗），而 `襄公 / 文侯 / 武王` 是谥号（〖@〗）。区别：
- `〖@X|太子Y〗` ✓ — X 是本名 Y 的简称，太子是身份
- `〖@X|邦国谥号X〗` ❌ — 邦国谥号是同一人完整别名，冗余

**canonical 与上下文邦国必须一致**（反面教训，2026-04-21 发现）

多国共享谥号时（如 灵公 可能是 晋/鲁/陈/宋 的任何一位），反思修正时务必**核对 canonical 与所在邦国列/段落一致**：

```
❌ 立〖◆陈〗〖@灵公|楚灵公〗子午        （陈国语境，canonical 误指楚灵公）
✅ 立〖◆陈〗〖@灵公|陈灵公〗子〖@午|妫午〗
```

检查方式：对每处新增/修改的 `〖@X|Y〗`，确认 Y 的邦国前缀与 X 所在的表列 / 段落主角邦国一致。

**扫描命令**（自查本库违规）：
```bash
python3 -c "
import re, os
pat = re.compile(r'〖@([^〗|]+)\|([^〗]+)〗')
titles = set('公侯王')
states = set('秦魏韩赵楚燕齐晋鲁卫宋郑蔡吴越蜀汉陈曹杞莒周代')
exc = ['太子','公子','王子','王女','公主']
for fn in sorted(os.listdir('chapter_md')):
    if not fn.endswith('.tagged.md'): continue
    with open(f'chapter_md/{fn}') as f: t = f.read()
    for m in pat.finditer(t):
        s, c = m.group(1), m.group(2)
        if len(c) <= len(s) or not c.endswith(s): continue
        p = c[:-len(s)]
        if len(p) < 2 or p[-1] not in titles: continue
        if any(p.endswith(x) for x in exc): continue
        if p[0] in states:
            print(f'{fn}: @{s}|{c}')
"
```

### 6.3 处理流程

```
源文本 @武王@
  → 消歧(chapter_level) → 周武王
  → 别名解析(global) → 周武王 (canonical，无进一步合并)

源文本 @沛公@
  → 消歧(无需) → 沛公
  → 别名解析 → 汉高祖 (canonical)

源文本 @厉王胡@
  → 消歧(无需) → 厉王胡
  → 别名解析 → 周厉王 (canonical)
```

---

## 七、覆盖率与改进方向

### 7.1 当前覆盖率（2026-04-19 最新）

| 指标 | 数值 | 变化 |
|------|------|------|
| RULER_DB 规则数 | 261 条（87 个歧义短名） | — |
| 已标注章节 | 130/130 | — |
| 已消歧章节 | 71/130（有歧义短名的章节全部覆盖）| — |
| 无需消歧章节 | 53/130（无歧义短名出现）| — |
| 章节级映射数（`disambiguation_map.json`）| **644** | — |
| 缺失消歧映射 | 6 条（同章多指代）| — |
| 幽灵条目（无效映射）| 0 条 | — |
| **别名条目（4 列结构，person）** | **3,322** | 旧版 591 → 新版 3,322（5.6×）|
| 别名唯一 surface | 2,137 | — |
| 别名唯一 canonical | 2,097 | — |
| 歧义 surface（多 canonical）| **550** | 新增维度 |
| `alias_conflicts.json` 记录 | 585 条 | 2026-04-19 首次导出 |
| Canonical 命名规范 | 汉帝=汉+通称、先秦=国+谥号 | — |
| 人名实体数 | **4,921** | 旧 4,418 → 新 4,921 |
| 全部实体数 | ~15,120（18 名词类）+ 211（4 动词类）| 口径调整 |

### 7.2 剩余覆盖缺口

经全量反思修正后，剩余6条无法章节级消歧（同章多指代）：

| 章节 | 短名 | 原因 |
|------|------|------|
| 005 | 平公 | 同章含晋平公(L226)和齐平公(L259) |
| 033 | 景公 | 同章含晋景公、齐景公、鲁景公 |
| 039 | 桓公 | 同章含曲沃桓叔和齐桓公 |
| 050 | 哀王 | 汉代封国王号，非先秦谥号体系 |
| 098 | 肃侯/顷侯 | 汉代侯爵谥号 |
| 111 | 哀侯/烈侯 | 汉代侯爵谥号（卫青后人） |

### 7.3 改进路径

**路径A：扩展RULER_DB**
- 补充缺失的 (短名, 国名) → 全名 映射
- 特别是侯爵体系（韩/赵/魏的侯爵称号）

**路径B：段落级消歧**
- 当前是章节级（一章一个映射），升级为段落级（每个段落独立消歧）
- 解决同一章节中短名指代切换的问题（如列传中叙述多国事务）

**路径C：AI辅助消歧**
- 对启发式无法覆盖的case，用LLM阅读上下文判断
- 输入：段落文本 + 候选全名列表 → 输出：最可能的全名
- 适合处理策略2-4都无法覆盖的复杂语境

**路径D：别名系统联动**
- 将 entity_aliases.json（别名→规范名）与消歧映射打通
- 例如"沛公"→"刘邦"是别名关系，不需要消歧
- "孝公"在秦本纪中→消歧→"秦孝公"→别名→"秦孝公"（规范名一致）

---

## 八、关键经验

### 8.1 消歧原则

1. **保守优于激进** — 不确定的case跳过不处理，错误消歧比缺失更有害
2. **不改源文件** — 消歧结果存储在独立JSON，渲染时注入，源文件是"真理之源"
3. **章节上下文是最强信号** — "周本纪"里的"武王"几乎肯定是周武王
4. **前缀国名最可靠** — `&秦&@昭王@` 的消歧置信度远高于窗口国名

### 8.2 工程实践

5. **RULER_DB要全** — 每个 (短名, 国名) 对都需要有映射，缺失导致消歧失败
6. **SKIP_NAMES要准** — 误加导致漏消歧，误删导致错误消歧
7. **MANUAL_CORRECTIONS是安全网** — 启发式永远有错，手动修正表是最后防线
8. **多数投票防震荡** — 同一章节同一短名的多次出现，取2/3多数决

### 8.3 古籍特殊性

9. **谥号是主要歧义源** — 先秦人物以谥号称呼是惯例，谥号字数少且重复率高
10. **国名是消歧关键** — 古文中国名几乎总是与人物短称同段出现
11. **体裁决定歧义密度** — 本纪/世家（单国叙事）歧义少，书/表/合传（跨国）歧义多
12. **同国同谥号极罕见** — 261条规则中几乎无重复，说明谥号制度本身有一定的去重机制

---

## 九、工具链

| 脚本 | 位置 | 功能 |
|------|------|------|
| `disambiguate_names.py` | kg/entities/scripts/ | 四层启发式消歧，生成 disambiguation_map.json |
| `review_disambiguation.py` | kg/entities/scripts/ | 消歧+别名反思审查工具（质检脚本） |
| `render_shiji_html.py` | 项目根 | 渲染时加载消歧映射，生成tooltip和链接 |
| `build_entity_index.py` | kg/entities/scripts/ | 构建实体索引（消歧后的规范名） |

| 数据文件 | 位置 | 说明 |
|----------|------|------|
| `disambiguation_map.json` | kg/entities/data/ | 章节级消歧映射（71章644条） |
| `entity_aliases.json` | kg/entities/data/ | 别名→规范名（591条，11个分类） |
| `entity_index.json` | kg/entities/data/ | 全量实体索引（17,483条实体，19类） |

---

---

## 十、君主实体的专项消歧

### 10.1 君主数据库（rulers.json）

**位置**：`kg/relations/rulers.json`
**规模**：656条，覆盖上古圣王至汉代藩王
**核心字段**：`所属国`、`谥号`、`名`、`别名`、`类型`、`登基年`、`退位年`

利用此数据库，可对87个歧义短名进行系统性查找：

```python
# 按(所属国, 谥号)精确匹配
def lookup_ruler(state, short_title, rulers_db):
    for r in rulers_db:
        if r['所属国'] == state and r['谥号'] == short_title:
            return r['谥号']  # 返回 canonical 全称（如"楚武王"）
    return None
```

### 10.2 在位年辅助消歧

当同一短名有多个候选（如"惠王"可能是秦惠王/魏惠王/燕惠王）时，可用上下文年份缩小范围：

```python
def disambig_by_year(short_name, context_year, rulers_db):
    """context_year 为公元前年份（负整数）"""
    candidates = []
    for r in rulers_db:
        if short_name in (r.get('谥号',''), r.get('名',''), *r.get('别名',[])):
            start = r.get('登基年') or -9999
            end   = r.get('退位年') or 9999
            if start <= context_year <= end:
                candidates.append(r)
    return candidates  # 通常缩减到1-2个候选
```

**示例**：

| 上下文年份 | 短名 | 在位区间匹配 | 消歧结果 |
|-----------|------|------------|---------|
| -334 | 惠王 | 秦惠王(-337~-311)、魏惠王(-369~-319)、燕惠王 | 需结合国名进一步缩小 |
| -279 | 昭王 | 秦昭王(-306~-251)、燕昭王(-311~-279) | 两者均在位，仍需国名 |
| -238 | 庄王 | 楚庄王(-613~-591) 不在位 → 楚幽王(-237~-228) | 年份排除法 |

### 10.3 谥号 vs 名 vs 别名的标注选择

rulers.json 中每位君主有多个字段，对应标注中的不同表现形式：

| 字段 | 典型值 | 史记中的出现形式 |
|------|--------|----------------|
| 谥号 | 楚武王 | 主要称谓，多见于本纪/世家 |
| 名 | 熊通 | 偶见于列传或初次介绍 |
| 别名 | 武王 | 章节内短称（需消歧） |

**标注建议**：按原文形式标注，消歧映射在 `disambiguation_map.json` 中维护，不修改源文件。

### 10.4 四级君主类型与消歧难度

| 类型 | 歧义程度 | 原因 |
|------|---------|------|
| 天子/皇帝 | 低 | 天子称谓少且唯一（"孝武帝"无歧义） |
| 诸侯王 | 高 | 多国并用"武王"/"昭王"等同谥（87个歧义短名主要在此类） |
| 诸侯侯君 | 高 | "桓公"/"献公"跨国重复率高，5+国共用同一谥号 |
| 汉诸侯王 | 低 | 封国+谥号在史记中唯一指代（"梁孝王"无歧义） |

### 10.5 消歧规则与rulers.json联动

RULER_DB（261条）中的 `(短名, 国名) → 全名` 映射规则，可从 rulers.json 自动生成初稿：

```python
def generate_ruler_db(rulers_json):
    """从rulers.json生成RULER_DB初稿"""
    ruler_db = {}
    for r in rulers_json:
        state = r['所属国']
        title = r.get('谥号', '')
        if title and state:
            short = title.replace(state, '')  # 去掉国名前缀得到短名
            if short and len(short) >= 2:
                ruler_db[(short, state)] = title
    return ruler_db
```

*注：自动生成的规则需人工审核，特别是吴国（寿梦等名字不含"王"字）和越国（允常/句践）的特殊情况。*

---

## 十一、别名数据 4 列结构（2026-04-19 上线）

### 11.1 数据格式

`entity_aliases.json` 从旧的 `{canonical: [alias...]}` 改为**按实体类型分片的 4 列表结构**：

```json
{
  "person": [
    {
      "surface": "昭王",
      "type": "person",
      "canonical": "秦昭王",
      "refs": [["005_秦本纪","20.1"], ["071_樗里子甘茂列传","3"]]
    },
    {
      "surface": "昭王",
      "type": "person",
      "canonical": "燕昭王",
      "refs": [["034_燕召公世家","17"]]
    }
  ],
  "place":    [...],
  "official": [...],
  "artifact": [...],
  "book": [...], "dynasty": [...], "feudal-state": [...],
  "identity": [...], "institution": [...], "legal": [...], "time": [...]
}
```

**关键属性**：

- **同 surface 多 canonical 允许并存**：歧义 surface 按 canonical 拆多行（"昭王" 3 行分别指 秦/燕/楚昭王）
- **refs 记录证据**：每条 (surface, canonical) 绑定到具体章节/段落，便于回查
- **类型分片**：跨类型不混（人名"汉"和邦国"汉"完全独立）

### 11.2 与旧结构的对比

| 维度 | 旧版（2026-03 前）| 新版（2026-04-19）|
|------|------------------|------------------|
| 数据结构 | `{canonical: [alias...]}` | 4 列表（surface/type/canonical/refs）|
| 歧义表达 | 无（同 surface 必须唯一映射）| 原生支持（多行）|
| 证据回查 | 无 | refs 字段 |
| 规模（person）| 591 | **3,322** |
| 保留旧版 | — | `entity_aliases.v1.bak.json` |

### 11.3 数据来源优先级

抽取时按以下顺序合并，前者优先：

1. **`tagged.md` 中的 `〖@X|Y〗` 内联消歧** — 最权威，作者实际标注
2. **`disambiguation_map.json`** — 章节级短名→全名（71 章 644 条）
3. **`rulers.json`** 的 `别名` 字段 — 656 条君主数据
4. **`private/to鲍捷 史记里的人名.md`** — 先秦称呼对照表，~100 条
5. **旧 `entity_aliases.json`** — 保底（source='legacy'）

**抽取脚本**：`kg/entities/scripts/extract_aliases_from_tags.py`
**输出**：`entity_aliases.json` + `entity_aliases.v1.bak.json` + `alias_conflicts.json`

### 11.4 冲突检测

`alias_conflicts.json` 用于质检，2026-04-19 导出包含 **585 个 ambiguous_surfaces**：

```json
{
  "ambiguous_surfaces": [
    {"type": "person", "surface": "万", "canonicals": ["南宫万", "韩武子"]},
    {"type": "person", "surface": "万石", "canonicals": ["石奋", "万石君"]}
  ]
}
```

**人工审查优先级**（按多义数降序，前 15 条见 §2.2 表）：
- canonical 数 ≥ 10：15 个短名，需全部审查
- canonical 数 4-9：123 个，抽样审查
- canonical 数 2-3：410 个，可交由启发式（四层策略）处理

### 11.5 渲染层行为

`render_shiji_html.py` 按以下顺序解析：

```
〖@武王〗
  ↓ 若有内联消歧 〖@武王|周武王〗 → canonical = "周武王"
  ↓ 否则查 disambiguation_map[章节][武王] → "周武王"
  ↓ 查 entity_aliases[person] 按 (surface=武王, refs 匹配当前章节) → "周武王"
  ↓ 兜底：保留 surface 为 canonical
→ 生成 HTML：<span class="person" title="人名：周武王">武王</span>
```

**注意**：新 4 列结构里 surface 歧义时（同"武王"对应周/秦/楚三人），渲染层需要**用 refs 字段过滤当前章节**来选择正确 canonical。这是与旧结构最大的行为差异。

---

## 十二、工作流与检查清单

每次新一轮标注完成后：

- [ ] 重跑 `extract_aliases_from_tags.py`，刷新 `entity_aliases.json`
- [ ] 对比 `entity_aliases.v1.bak.json` 审查大幅度变化
- [ ] 打开 `alias_conflicts.json`，处理 ≥5 canonical 的高歧义短名
- [ ] 若新发现歧义短名不在 `RULER_DB`，补入 261 条规则表
- [ ] 重跑 `disambiguate_names.py` 刷新 `disambiguation_map.json`
- [ ] 用 `review_disambiguation.py` 抽样验证（100 条随机），错误率应 < 2%
- [ ] 按 SKILL_06e 流程把新增 canonical 追加到 `person.ttl`
- [ ] 生成的 HTML tooltip 能正确显示 canonical

---

*本节基于 656 条 rulers.json 数据（2026-03 整理完成），覆盖上古至汉代全部君主。*
*相关数据文件：`kg/relations/rulers.json`、`kg/entities/data/entity_aliases.json`、`kg/entities/data/alias_conflicts.json`、`kg/entities/data/disambiguation_map.json`*
*整理过程详见：`doc/entities/特殊任务/史记君主列表整理过程.md`*

**关键时间线**：
- 2026-03-09：全量反思+修正、补标 035-040 六章（3780 处标记）、+25 canonical 调整、+204 消歧映射
- 2026-03-13（v2.0）：9 类对称符号迁移为 `〖〗` 格式（`@人名@`→`〖@人名〗`，`&朝代&`→`〖&朝代〗` 等）
- 2026-03-17（v2.7）：内联消歧 `〖TYPE X|Y〗` 扩展到全部 13 种实体类型
- **2026-04-19（v3.0）**：`entity_aliases.json` 升级 4 列结构；人名别名 591 → 3,322；导出 `alias_conflicts.json`（585 条歧义短名）

*反思详情：[doc/entities/实体分类修正/实体消歧别名反思报告.md](../doc/entities/实体分类修正/实体消歧别名反思报告.md)*

> 另见：
> - [SKILL_03j_人名分类.md](SKILL_03j_人名分类.md) — 人名 16 类身份分类（与消歧协同工作）
> - [SKILL_04e_年份消歧.md](SKILL_04e_年份消歧.md) — 在位纪年→公元年的消歧方法论
> - [SKILL_06e_分类树总结.md](SKILL_06e_分类树总结.md) — 分类树增量维护与缺口分析
